# Store 边界

房间、公屏、转写和总结只能通过 Store 接口访问。当前版本在 `memory` 中提供内存实现；后续需要多实例或持久化时，可在不修改领域规则的前提下替换为 Redis 或数据库实现。

- `ChatStore` 保存房间公屏消息。
- `SpeechTurnStore` 保存发言生命周期、点赞数和转写状态。
- 列表接口首屏返回最近一页，页内按时间正序排列；`nextCursor` 用于继续读取更早记录。
